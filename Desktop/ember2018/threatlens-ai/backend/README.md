# Backend Placeholder

This module will consume the ML Engine through MalwareScanner.scan().

Future work:
- expose a FastAPI endpoint
- integrate the scanner with request/response handling
- connect the frontend to this service
